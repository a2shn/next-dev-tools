import type { NextConfig } from 'next'
import { withNextDevTools } from '@next-dev-tools/core'

const nextConfig: NextConfig = {
  /* config options here */
}

export default withNextDevTools(nextConfig)
